# 0 "3.c"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "3.c"





int main(){
    double radius = 5.0;
    int x = 10;
    int y = 20;
    double cir = radius * radius * 3.14159;
    int larger_num = ((x) > (y) ? (x) : (y));
    return 0;
}
